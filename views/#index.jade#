doctype html

html
    head
    title Team #{teamNumber}
body
    h1 Database Version
    p #{databaseVersion}

    h2 Team Members

    // slide 51 begin HTML template
    form(name="add-team-member", method="post")
        div.input
            span.label First Name
            input(type="text",name="first_name")

        div.input
            span.label Last Name
            input(type="text",name="last_name")

        div.actions
        input(type="submit",value="add")

    bry
    // slide 51 end

    table
        thead
            tr
                td First Name
                td Last Name
        tbody
            each teamMember in teamMembers
                tr
                    td #{teamMember.last_name}
                    td #{teamMember.first_name}